/**
 * Shared admin sidebar group labels, bilingual (English base, Turkish translation).
 * Import the same object everywhere so Payload merges collections under one group.
 */
export const GROUP_CONTENT = { en: 'Content', tr: 'İçerik' }
export const GROUP_FORMS = { en: 'Forms', tr: 'Form' }
