/**
 * Custom Payload admin logo, shown on the login screen and the nav.
 * Renders the katalog brand mark served from `public/sistem-katalog.png`.
 */
export const Logo = () => (
  <img
    src="/sistem-katalog.png"
    alt="Sistem Granit"
    style={{ maxWidth: '12rem', width: '100%', height: 'auto' }}
  />
)
